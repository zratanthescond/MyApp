import { useTheme } from "@/theme";
const { colors, fonts, gutters, layout, backgrounds, borders } = useTheme();
export const TextHeader = [
  fonts.bold,
  fonts.size_24,
  fonts.gray800,
  {
    fontFamily: "Lato",
  },
];
